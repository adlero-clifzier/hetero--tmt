package com.hetero.model;

/**
 * Enumeration of task priority levels, ordered from highest to lowest urgency.
 * Used for sorting, filtering, and visual differentiation in the UI.
 */
public enum Priority {
    CRITICAL,
    HIGH,
    MEDIUM,
    LOW,
    MINIMAL
}
